#ifndef VAR_ARGS_H
#define VAR_ARGS_H

#include <stdarg.h>
#include <stdio.h>
#include <limits.h>

// Прототипы функций
int sum(int count, ...);
int max(int count, ...);
int min(int count, ...);
double average(int count, ...);

#endif // VAR_ARGS_H
